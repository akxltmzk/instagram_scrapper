extends layout

block content 
  .content.mobile
    .contents-box
      .title INSTAGRAM SCRAPPER
      .form-wrapper 
        form#initailize-form(action='/initialize-contents')
          input.block(type='text' id='instaID' name='instaID' placeholder='Enter your ID')
          button(type='submit') START

    .loader-box.hidden
      .label LOADING ...
      .loader Loading    

    .control-pannel.hidden
      .title FUNCTION SELECT
      .form-wrapper 
        form#downloadimage
          button(type='submit') download image   
        form#backtointro
          button(type='submit') back to intro        

  script(src='js/index.js')